spring:
  application:
    name: config-server
  
  profiles:
    active: git

  cloud:
    config:
      server:
        git:
          uri: https://github.com/bishmie/campusdocs-cloud-configurations.git
          search-paths: platform,services
          clone-on-start: true

server:
  port: 8888
